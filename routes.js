const HOME = "/";
const SEARCH = "/search";
const SEARCHED = "/:id/searched";
const ADMIN = "/admin";

const routes = {
  home: HOME,
  search: SEARCH,
  searched: SEARCHED,
  admin: ADMIN,
};

export default routes;
