//Home
const HOME = "/";

//Api
const API = "/api";
const SEARCH = "/search";
const UPDATE = "/:id/update";

//Admin
const ADMIN = "/admin";
const LOGIN = "/login";

const routes = {
  home: HOME,
  api: API,
  search: SEARCH,
  update: UPDATE,
  admin: ADMIN,
  login: LOGIN,
};

export default routes;
